The dataset card is generated by `chaashini/cardgen.py` and refreshed on every push (per-language
table, totals, configs for `load_dataset(repo, "<lang>")`). It deliberately does not name the
source platform or the models used; it describes the corpus, the quality gates in generic terms,
the schema, and usage. Run `python -m chaashini card` to print the current card.
