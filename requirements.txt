# FlowTTS (OmniVoice) requirements
#
# Target: single NVIDIA H200 (Hopper), Linux, Python 3.10–3.12.
# The sglang + ncodec + vllm MiraTTS stack was removed; this serves
# k2-fsa/OmniVoice via the native `omnivoice` package.
#
# NOTE: torch / CUDA wheels are platform-specific — install the CUDA build that
# matches the H200 image FIRST (e.g. `pip install torch==2.8.0 --index-url
# https://download.pytorch.org/whl/cu128`), THEN `pip install -r requirements.txt`.
# Pin exact versions against your box before production (see plan verification).

# ── OmniVoice model + core inference ─────────────────────────────────────────
omnivoice==0.1.5
transformers>=5.3.0          # ships HiggsAudioV2TokenizerModel (OmniVoice codec)
torch>=2.4
torchaudio>=2.4
accelerate>=1.0
safetensors>=0.4
tokenizers>=0.20
huggingface-hub>=0.30
hf-transfer>=0.1.9

# ── Audio ────────────────────────────────────────────────────────────────────
librosa>=0.10
soundfile>=0.12
soxr>=0.5
pydub>=0.25

# ── WebSocket server + HTTP control API ──────────────────────────────────────
websockets>=13.0
aiohttp>=3.10

# ── FastAPI / HTTP (Redis-backed secondary path: main.py) ────────────────────
fastapi>=0.110
uvicorn>=0.30
starlette>=0.37

# ── Redis (secondary multi-process path) ─────────────────────────────────────
redis>=5.0

# ── Config / validation ──────────────────────────────────────────────────────
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ── Logging / monitoring ─────────────────────────────────────────────────────
structlog>=24.1
prometheus-client>=0.20

# ── Numerics ─────────────────────────────────────────────────────────────────
numpy>=1.26
scipy>=1.11
