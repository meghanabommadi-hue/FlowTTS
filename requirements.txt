# FlowTTS gateway requirements (CPU-only).
#
# The GPU TTS model (Fish Audio S2 Pro) runs in the SEPARATE sglang-omni backend
# container (see docker/fish_s2pro.Dockerfile) — it brings its own torch/sglang/CUDA
# stack. This gateway is a lightweight async WebSocket proxy: no torch, no CUDA.

# ── HTTP client to the sglang backend + WebSocket server + control API ───────
aiohttp>=3.10
websockets>=13.0

# ── Audio I/O / conversion ───────────────────────────────────────────────────
numpy>=1.26
soundfile>=0.12          # WAV encode (decoder.py) + cache files
pydub>=0.25              # reference-clip → mono WAV conversion (via ffmpeg)

# ── FastAPI / HTTP (Redis-backed secondary path: main.py) ────────────────────
fastapi>=0.110
uvicorn>=0.30
starlette>=0.37

# ── Redis (secondary multi-process path) ─────────────────────────────────────
redis>=5.0

# ── Config / validation ──────────────────────────────────────────────────────
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ── Logging / monitoring ─────────────────────────────────────────────────────
structlog>=24.1
prometheus-client>=0.20
